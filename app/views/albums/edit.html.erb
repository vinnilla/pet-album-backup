<%= form_for(@album, url: update_album_path) do |f| %>

  <% if @album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@album.errors.count, "error") %> prohibited this album from being saved:</h2>

      <ul>
      <% @album.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form_box form-horizontal">
    <h1>Edit Album</h1>
    <div class="form-group first">
      <%= f.label "Title", class:"col-md-3 lower"%>
      <div class="col-md-9">      
        <%= f.text_field :title, class:"form-control", autofocus: true, autocomplete: "off" %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label "Description", class:"col-md-3 lower" %>
      <div class="col-md-9">      
        <%= f.text_field :description, class:"form-control", autocomplete: "off" %>
      </div>
    </div>

    <div class="form-group">
      <%= f.submit "Save", class: "button"%>
    </div>
  </div>

<% end %>